#pragma once

#include <vector>
#include <string>
#include <iostream>
#include <iterator>
#include <algorithm>

namespace protein_translation {
    std::vector<std::string> proteins(std::string);
}  // namespace protein_translation